import { routes } from './app.routes';
describe('Demo navigation',()=>{it('contains the complete student flow',()=>{const paths=routes.map(r=>r.path);expect(paths).toContain('aluno/jornada');expect(paths).toContain('aluno/instrutores');expect(paths).toContain('aluno/instrutores/:id');expect(paths).toContain('aluno/solicitar');expect(paths).toContain('aluno/matching');expect(paths).toContain('aluno/clinicas')});it('contains the professional instructor and demand flow',()=>{const paths=routes.map(r=>r.path);expect(paths).toContain('profissional');expect(paths).toContain('profissional/instrutor');expect(paths).toContain('profissional/demanda')})});
