import { HttpClient } from '@angular/common/http';
import { Component, inject } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, Router, RouterLink } from '@angular/router';

@Component({selector:'app-lesson-request',imports:[FormsModule,RouterLink],template:`<section class="page narrow"><p class="eyebrow">Solicitar aula</p><h1>Envie sua solicitação</h1>@if(needsLogin){<p>Entre ou crie uma conta para continuar. O instrutor selecionado será preservado.</p><a class="button primary" routerLink="/entrar" [queryParams]="{returnUrl:returnUrl}">Entrar</a><a class="button secondary" routerLink="/cadastro/aluno" [queryParams]="{returnUrl:returnUrl}">Criar conta</a>}@else if(success){<p role="status">Solicitação registrada com sucesso.</p><a routerLink="/aluno/painel">Acompanhar no painel</a>}@else{<form class="form" (ngSubmit)="submit()"><label>Categoria<select name="category" [(ngModel)]="category"><option value="B">Categoria B</option></select></label><label>Período preferido<input name="period" [(ngModel)]="period" maxlength="80"></label><label class="full">Mensagem<textarea name="message" [(ngModel)]="message" maxlength="500"></textarea></label><button class="button primary full" [disabled]="sending">Enviar solicitação</button></form>@if(error){<p role="alert">Não foi possível registrar a solicitação.</p>}}</section>`})
export class LessonRequestComponent{private http=inject(HttpClient);private router=inject(Router);private route=inject(ActivatedRoute);instructor=this.route.snapshot.queryParamMap.get('instrutor')||'';category='B';period='';message='';sending=false;success=false;error=false;needsLogin=false;returnUrl=this.router.url;
submit(){if(!this.instructor){this.error=true;return}this.sending=true;this.http.post('/demo/marketplace/lesson-requests/',{instructor_id:this.instructor,category:this.category,preferred_period:this.period,message:this.message}).subscribe({next:()=>{this.success=true;this.sending=false},error:e=>{this.sending=false;if(e.status===401||e.status===403)this.needsLogin=true;else this.error=true}})} }
