import { TestBed } from '@angular/core/testing';import { DemoClinicProvider,DemoDemandProvider,DemoInstructorProvider,DemoJourneyProvider } from './demo-data.providers';
describe('Synthetic demo providers',()=>{beforeEach(()=>TestBed.configureTestingModule({}));it('isolates a complete fictitious journey',()=>{const p=TestBed.inject(DemoJourneyProvider);expect(p.steps.length).toBe(8);expect(p.student.name).toContain('Demo')});it('contains explicitly demonstrative providers and mock scores',()=>{const i=TestBed.inject(DemoInstructorProvider).instructors;const c=TestBed.inject(DemoClinicProvider).clinics;expect(i.every(x=>x.name.includes('Demo'))).toBeTrue();expect(c.every(x=>x.name.includes('Demo'))).toBeTrue();expect(i.map(x=>x.match)).toEqual([96,91,87])});it('uses only the five visual priority states',()=>expect(TestBed.inject(DemoDemandProvider).states.map(x=>x.uf)).toEqual(['RS','SC','SP','RJ','ES']))});
