import { TestBed } from '@angular/core/testing'; import { provideRouter } from '@angular/router'; import { AppComponent } from './app.component'; import { routes } from './app.routes';
describe('InstrutorPro demo shell',()=>{beforeEach(async()=>TestBed.configureTestingModule({imports:[AppComponent],providers:[provideRouter(routes)]}).compileComponents());it('renders the InstrutorPro identity and logo',()=>{const f=TestBed.createComponent(AppComponent);f.detectChanges();const root=f.nativeElement;expect(root.textContent).toContain('InstrutorPro');expect(root.textContent).not.toContain('EMBREA');expect(root.querySelector('.brand img').getAttribute('src')).toBe('/logo.jpg')});it('offers student and professional navigation',()=>{const f=TestBed.createComponent(AppComponent);f.detectChanges();const hrefs=[...f.nativeElement.querySelectorAll('nav a')].map((a:any)=>a.getAttribute('href'));expect(hrefs).toContain('/aluno/jornada');expect(hrefs).toContain('/profissional')})});
