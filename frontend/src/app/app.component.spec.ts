import { TestBed } from '@angular/core/testing'; import { provideRouter } from '@angular/router'; import { AppComponent } from './app.component'; import { routes } from './app.routes';
describe('InstrutorProCNH shell',()=>{beforeEach(async()=>TestBed.configureTestingModule({imports:[AppComponent],providers:[provideRouter(routes)]}).compileComponents());it('renders the InstrutorProCNH identity and logo',()=>{const f=TestBed.createComponent(AppComponent);f.detectChanges();const root=f.nativeElement;expect(root.textContent).toContain('InstrutorProCNH');expect(root.textContent).not.toContain('EMBREA');expect(root.querySelector('.brand img').getAttribute('src')).toBe('/logo-cnh.svg')});it('offers the professional public navigation',()=>{const f=TestBed.createComponent(AppComponent);f.detectChanges();const hrefs=[...f.nativeElement.querySelectorAll('nav a')].map((a:any)=>a.getAttribute('href'));expect(hrefs).toContain('/aluno/instrutores');expect(hrefs).toContain('/profissional/instrutor/entrada');expect(hrefs).toContain('/cadastro/aluno')})});
